{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build debug just",
            "command": "${workspaceFolder}/.vscode/build-debug-just.sh",
            "args": [
                "/tmp/just_debug_build"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
